{
  "name": "todo-app-publisher",
  "version": "1.0.0",
  "description": "## Getting started",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:dev": "tsc --build",
    "start": "node ./build/main.js",
    "migrate:dev": "npx prisma migrate dev"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.2"
  },
  "devDependencies": {
    "@prisma/client": "^5.6.0",
    "prisma": "^5.6.0"
  }
}
